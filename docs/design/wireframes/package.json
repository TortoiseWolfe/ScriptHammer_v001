{
  "name": "firstframe-wireframes",
  "version": "1.0.0",
  "private": true,
  "description": "Interactive wireframe viewer for FirstFrame template",
  "scripts": {
    "dev": "live-server --port=3000",
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "live-server": "^1.2.2"
  }
}
